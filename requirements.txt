# Core data
yfinance>=0.2.51
pandas>=2.0.0
numpy>=1.24.0

# Storage
SQLAlchemy>=2.0.0       # used for PostgreSQL path if needed
# Turso: uses HTTP API via httpx — no native driver needed

# Config & env
python-dotenv>=1.0.0
PyYAML>=6.0.0

# API
fastapi>=0.111.0
uvicorn>=0.29.0
pydantic[email]>=2.0.0
httpx>=0.27.0

# HTTP
requests>=2.31.0

# Notifications
resend>=2.0.0
jinja2>=3.1.0
mjml>=0.12.0

# CLI output
tabulate>=0.9.0

# v2 — AI enrichment
google-generativeai>=0.7.0  # Gemini AI
slowapi>=0.1.9           # rate limiting (already on API, needed for GitHub Actions too)
